<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:ui="clr-namespace:Joker.UserInterface"
			 x:Class="Joker.UserInterface.PictureFeed"
			 Icon="ui_pictures.png">
	<ContentPage.Title>
		<OnPlatform iOS="{Static ui:PictureFeed.Headline}" />
	</ContentPage.Title>
	<Grid RowSpacing="0"
		  ColumnSpacing="0"
		  HorizontalOptions="FillAndExpand"
		  VerticalOptions="FillAndExpand">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		<Frame Grid.Row="0"
			   Grid.Column="0"
			   Grid.ColumnSpan="2"
			   BackgroundColor="#333333"
			   HorizontalOptions="FillAndExpand"
			   VerticalOptions="FillAndExpand"
			   CornerRadius="0"
			   Padding="0">
			<Image x:Name="Picture"
				   Source="{Binding PresentedImage}">
				<Image.GestureRecognizers>
					<TapGestureRecognizer Command="{Binding SetNextPicture}"
										  NumberOfTapsRequired="2" />
				</Image.GestureRecognizers>
			</Image>
		</Frame>
		<Button Command="{Binding ToggleLikedStatus}"
				Grid.Row="1"
				Grid.Column="0"
				Image="{Binding LikeButtonImage}"
				Text="{Binding LikeButtonText}"
				BackgroundColor="{Binding LikeButtonBackgroundColor}"
				TextColor="{StaticResource Text1}"
				ContentLayout="Left,30"
				CornerRadius="0" />
		<Button Command="{Binding SavePictureToGallery}"
				Grid.Row="1"
				Grid.Column="1"
				Image="ui_save.png"
				BackgroundColor="{StaticResource Bgr4}"
				CornerRadius="0">
			<Button.Padding>
				<OnPlatform Android="{StaticResource PagePadding}"
							iOS="25" />
			</Button.Padding>
		</Button>
	</Grid>
</ContentPage>